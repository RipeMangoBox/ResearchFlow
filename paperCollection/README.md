# paperCollection/

**Read-only export.** Generated index + Obsidian navigation pages. Rebuilt by `papers-build-collection-index` skill.

For programmatic queries, use backend API: `POST /api/v1/search/hybrid`

Contents (gitignored, generated on demand):
- `index.jsonl` — flat JSON index for fast filtering
- `by_task/`, `by_technique/`, `by_venue/` — Obsidian navigation pages
- `_AllPapers.md` — grouped paper listing
