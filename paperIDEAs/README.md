# paperIDEAs/

Research output notes. Generated by brainstorming skills, also stored in DB (`idea_deltas` + `delta_cards` tables).

API: `POST /api/v1/search/ideas` or `GET /api/v1/graph/ideas/{paper_id}`
